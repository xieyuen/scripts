class FormatKeys:
    ELAPSED = "{elapsed}"
    EXCEPTION = "{exception}"
    EXTRA = "{extra}"
    FILE = "{file}"
    FUNCTION = "{function}"
    LEVEL = "{level}"
    LINE = "{line}"
    MESSAGE = "{message}"
    MODULE = "{module}"
    NAME = "{name}"
    PROCESS = "{process}"
    THREAD = "{thread}"


FORMAT = '[{time:YYYY-MM-DD HH:mm:ss}] [{level}] [{module}] | {message}'
